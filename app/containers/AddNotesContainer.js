var React = require('react')
var AddNotes = require('../components/AddNotes');

var AddNotesContainer = React.createClass({
    render: function () {
        return (
            <AddNotes />
        );
    }
});

module.exports = AddNotesContainer;
